# Playwright
test-results/
playwright-report/
playwright-screenshots/
playwright/.cache/
blob-report/
all-blob-reports/

# Native build output (regenerated by expo prebuild)
android/
ios/

# Firebase client config: copied from apps/web/android/app/ locally,
# written in CI from secrets.GOOGLE_SERVICES_JSON. Never committed.
google-services.json
