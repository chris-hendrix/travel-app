/**
 * The app's home.
 *
 * The trips list is still under construction under /design, so this is
 * a re-export rather than a second copy — the same promotion the profile
 * screen took. The chrome's wordmark points here, because the wordmark
 * means "home" and home is the list of trips, not the landing: the
 * landing is what you read before you have any.
 */
export { default } from "./design/trips";
